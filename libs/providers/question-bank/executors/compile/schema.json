{
  "$schema": "http://json-schema.org/schema",
  "version": 2,
  "title": "Compile",
  "description": "Compiles a question bank generating json files ready to consume by the client at runtime",
  "type": "object",
  "properties": {},
  "required": []
}
