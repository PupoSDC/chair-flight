# chair-flight-question-bank-framework

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build chair-flight-question-bank-framework` to build the library.

## Running unit tests

Run `nx test chair-flight-question-bank-framework` to execute the unit tests via [Jest](https://jestjs.io).
