import { Canvas, Meta, Story } from "@storybook/blocks";
import {
  Title,
  Subtitle,
  Description,
  Primary,
  Controls,
  Stories,
} from "@storybook/blocks";
import * as AppLayoutStories from "./app-layout.stories";

<Meta of={AppLayoutStories} />

<Title />

CSS building blocks to build consistent page layouts for the more "app-like"
pages.
