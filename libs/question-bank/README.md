# question-bank

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build question-bank` to build the library.

## Running unit tests

Run `nx test question-bank` to execute the unit tests via [Jest](https://jestjs.io).
